name: tv_series
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  chewie: ^1.7.5
  flutter:
    sdk: flutter
  flutter_login_template: ^1.1.0
  http: ^1.1.0
  video_player: ^2.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0




flutter:
  uses-material-design: true
  assets:
    - assets/videos/
